*.pyc
.pytest_cache/
.ruff_cache/
.venv/
__pycache__/
docs/server.reference.js
.superpowers/
# Local agent/editor tooling config (launch profiles etc). Internal only, like the two above.
.claude/

# Superpowers process docs (specs/plans) — internal only, never on the public repo
docs/superpowers/

# Agent instructions — internal only. Ignored rather than excluded per-clone so it cannot be
# re-added from another machine; it was tracked here until 1.3.5 and is still in the history.
CLAUDE.md
# stremio-loop artifacts staged into this checkout for the gated release —
# probe captures and monitor runtime state, never part of the product.
compat/out/
monitor/state/
